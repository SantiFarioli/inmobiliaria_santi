@model inmobiliaria_santi.Models.Inquilino

@{
    ViewData["Title"] = "Detalle del Inquilino";
}

<div class="text-center mb-4">
    <h1>Detalle del Inquilino</h1>
</div>

<div class="d-flex justify-content-center">
    <div class="card shadow-sm text-center" style="max-width: 500px; width: 100%;">
        <div class="card-body">
            <h5 class="mb-3 fw-bold">@Model.nombre @Model.apellido</h5>

            <p><strong>DNI:</strong> @Model.dni</p>
            <p><strong>Teléfono:</strong> @Model.telefono</p>
            <p><strong>Email:</strong> @Model.email</p>

            <div class="text-center mt-4">
                <a asp-action="Index" class="btn btn-secondary">🔙 Volver</a>
            </div>
        </div>
    </div>
</div>
